<!DOCTYPE html>
<html>
    <head> <!--Contenedor de metadata y otras cosas como el titulo-->
        <meta charset="utf-8"> <!--Definir los caracteres a utilizar-->
        <link rel = "stylesheet" href = "/css/UnirseComunidad.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
        <title>Consulta de Incidentes</title>
    </head>

    <body id="body">
        <header id="main-header">
            <a id="logo-header" href="#">
                <span class="site-name">Serviño</span>
                <span class="site-desc">Servicios/ Comunidades / Confianza</span>
            </a>


            <nav>
                <ul>
                    <li class="nav-item"><a href="/menu">Inicio</a></li>
                    {{#if usuarioBasico}}
                    <li class="nav-item"><a href="/incidentes">Mis Incidentes</a></li>
                    <li class="nav-item"><a href="/sugerenciasDeRevision">Sugerencias de Revisión</a></li>
                    {{/if}}
                    <li class="nav-item"><a href="/perfil/{{usuario_id}}"><img src="/./images/userLogo2.png" alt="userImage" id="userImage" width="30" height="30"></a></li>
                </ul>
            </nav>
        </header>
        <div id="base">
            <h1>COMUNIDADES</h1>
                <div class="row row-cols-1 row-cols-md-2 g-4">
                {{#each comunidades}}
                    <div class="col">
                        <div class="tarjetaComunidad d-flex justify-content-between align-items-center">
                            <div>
                                <h2>{{this.nombre}}</h2>
                                <h4>Miembros: {{this.cantidadMiembros}}</h4>
                            </div>
                            {{#if usuarioBasico}}
                            <button type="button" class="btn btn-secondary btn-unirse" data-comunidad-id="{{this.id}}">Unirme</button>
                            {{else}}
                            <button type="button" class="btn btn-secondary btn-eliminar" data-comunidad-id="{{this.id}}">Eliminar</button>
                            {{/if}}
                        </div>
                    </div>
                {{/each}}
                </div>
        </div>
        <script src="/js/EliminarComunidad.js"></script>
        <script src="/js/UnirseAComunidad.js"></script>
    </body>
</html>
